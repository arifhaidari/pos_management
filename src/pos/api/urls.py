# from django.urls import path, reverse_lazy

# # from .views import PosHome
# from .views import (
#      PosBackupAPIView, PosBackupDetailAPIView
#      )
     
     

# app_name = "pos"


# urlpatterns = [
#     path('', PosBackupAPIView.as_view(), name="pos_list"),
#     path('<int:id>/', PosBackupDetailAPIView.as_view(), name="pos_detail"),
# ]
